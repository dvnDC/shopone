<!--<li id="item" -<%#= item.id %>>-->
<%#= link_to gravatar_for(item.user, size: 50), item.user %>
<!--  <span class="user"><%#= link_to item.user.name, item.user %></span>-->
<!--  <span class="content">-->
<%#= item.content %>
<%#= image_tag item.display_image if item.image.attached? %>
<!--  </span>-->
<!--  <span class="timestamp">-->
<!--Posted <%#= time_ago_in_words(item.created_at) %> ago.-->
<%# if current_user?(item.user) %>
<%#= link_to "delete", item, method: :delete, data: { confirm: "You sure, dude?" } %>
<%# end %>
<!--</span>-->
<!--</li>-->

<li id="items" -<%= item.id %>>
  <%= item.name %>
  <%= item.price %>
  <%= item.content %>
  <%= item.content %>
  <%= image_tag item.display_image if item.image.attached? %>
  Posted <%= time_ago_in_words(item.created_at) %> ago.
  <% if current_user?(item.user) %>
    <%= link_to "delete", item, method: :delete, data: { confirm: "Are you sure?" } %>
  <% end %>
  </span>
</li>