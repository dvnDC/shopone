<div class="container-xxl position-relative p-0">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 px-lg-5 py-3 py-lg-0">
    <a href="/" class="navbar-brand p-0">
      <h1 class="text-primary m-0">
        <i class="fa fa-gem"></i>
        Shopone
      </h1>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
      <span class="fa fa-bars"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <div class="navbar-nav ms-auto py-0 pe-4">
        <%= link_to "Home", root_path, class: "nav-item nav-link #{is_active(root_path)}" %>
        <%= link_to "Help", help_path, class: "nav-item nav-link #{is_active(help_path)}" %>
        <%= link_to "About us", about_path, class: "nav-item nav-link #{is_active(about_path)}" %>
        <%= link_to "Contact", contact_path, class: "nav-item nav-link #{is_active(contact_path)}" %>
        <%= link_to "Search", search_path, class: "nav-item nav-link #{is_active(search_path)}" %>
        <%= link_to "Products", items_path, class: "nav-item nav-link #{is_active(items_path)}" %>
        <% if logged_in? %>
          <div class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Profile</a>
            <div class="dropdown-menu m-0">
              <%= link_to "Profile", current_user, class: "dropdown-item" %>
              <%= link_to "Settings", edit_user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Friends", friends_path, class: "dropdown-item" %>
              <%= link_to "Sign out", logout_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "dropdown-item" %>
            </div>
          </div>
          </div>
          <form class="d-flex">
            <button class="btn btn-outline-warning" type="submit">
              <i class="bi-cart-fill me-1"></i>
              Cart
              <span class="badge bg-warning text-black ms-1 rounded-pill">0</span>
            </button>
          </form>
        <% end %>
        </div>
    <%= link_to "SIGN IN", login_path, class: "btn btn-primary py-2 px-4" unless logged_in? %>
  </nav>
</div>